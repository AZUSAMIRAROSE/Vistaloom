version: '3.8'

services:
  db:
    image: postgres:16
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: vistaloom
      POSTGRES_PASSWORD: password
      POSTGRES_DB: vistaloom
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
